This is my Vim config.

Instructions:

1. Download this and extract it under a ".vim" folder, inside your home directory.
2. Create a symbolic link for ".vimrc" file using the following command:

ln -s ~/.vim/.vimrc ~/.vimrc
